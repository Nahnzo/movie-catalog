export const getCollectionLength = (state) => state?.myCollection?.myCollection.length ?? 0;
export const getReviewsLength = (state) => state?.arrayReviews?.arrayReviews.length ?? 0;
export const getWantToSeeLength = (state) => state?.wantToSee?.wantToSee.length ?? 0;
