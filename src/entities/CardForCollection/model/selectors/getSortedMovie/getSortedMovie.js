export const getSortedMovie = (state) =>
  state.arrayReviews.movies.filter((item) => item.id === movie.id).filter((item) => item.myReviews);
//   .filter((item) => item.id === movie.id).filter((item) => item.myReviews);
